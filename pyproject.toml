[project]
name = "motovj"
version = "3.0.0"
description = "MotoVJ v3 — Node-based VJ Software"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
]

[project.scripts]
motovj = "nt.start:main"
